1. Install dependencies:
   `npm install`
2. Set the `QWEN_MODEL` in [.env.local](.env.local)
   
   纯前端项目 调用 qwen 有 CORS 跨域问题 可自行查看 qwenService.ts 写 Server.js 实现本地转发端口
3. Run the app:
   `npm run dev`
