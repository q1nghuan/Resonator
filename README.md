1. Install dependencies:
   `npm install`
2. Set the `QWEN_API_KEY` in [.env.local](.env.local) to your Qwen API key
   - 可选：设置 `QWEN_API_BASE_URL` 自定义API端点（默认使用DashScope）
3. Run the app:
   `npm run dev`
